# Phase 3 Validation — Interactive Bubble Tea TUI

Pass/fail conditions. Read at phase completion, not during implementation.

- [ ] PASS iff `grotto tui` launches the run-list screen and `q` quits cleanly.
- [ ] PASS iff enter on a run opens the waterfall screen rendering proportional bars.
- [ ] PASS iff expand/collapse + scroll respond in under 50 ms per keystroke on a 200-span trace.
- [ ] PASS iff enter on a span opens the inspector showing typed attributes; esc returns to the waterfall.
- [ ] PASS iff `teatest` model tests assert correct screen transitions on key messages.

FAIL on any unchecked box → fix before advancing to Phase 4. This is the second shippable checkpoint (90% cumulative personal-use value).

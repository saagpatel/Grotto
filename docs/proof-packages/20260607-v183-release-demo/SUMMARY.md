# Grotto v1.8.3 Release Demo Proof Package

Status: passed.

This package is the release/demo exemplar. It wraps the existing release handoff,
checksums, CI gates, README demo path, and JUnit adapter screenshot asset.

For a future release, refresh this package with fresh command receipts from the
build, test, lint, checksum, and install smoke path.
